apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-frontend-helm.fullname" . }}-test-connection"
  labels:
    {{- include "app-frontend-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-frontend-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
